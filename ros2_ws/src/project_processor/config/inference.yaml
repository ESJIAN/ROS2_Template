# ================================================================
# inference.yaml — 推理节点参数
# ================================================================

inference_node:
  ros__parameters:
    topic_in: /sensor/processed
    topic_out: /result/inference
    model_name: threshold       # threshold | svm | neural_net
    confidence_threshold: 0.5
    model_path: ""              # 模型文件路径（SVM/深度学习时填写）
