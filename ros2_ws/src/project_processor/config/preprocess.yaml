# ================================================================
# preprocess.yaml — 预处理节点参数
# ================================================================

preprocessor_node:
  ros__parameters:
    topic_in: /sensor/raw
    topic_out: /sensor/processed
    normalize: true
    buffer_size: 100
