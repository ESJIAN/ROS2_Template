# ================================================================
# visualizer.yaml — 可视化节点参数
# ================================================================

visualizer_node:
  ros__parameters:
    topic_raw: /sensor/raw
    topic_processed: /sensor/processed
    topic_inference: /result/inference
    buffer_size: 500
    update_rate_hz: 10.0

dashboard_node:
  ros__parameters:
    print_rate_hz: 1.0
